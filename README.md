# 🤖 Telegram Parser Bot (поиск заказов для определенной специальности)

## 📌 Назначение проекта

Этот проект — **Telegram-бот для автоматического мониторинга и парсинга новых заказов** на веб‑платформе Profi.ru и отправки уведомлений в Telegram.

Бот предназначен для ситуаций, когда:

* заказы публикуются на сайте без API;
* нужно **отслеживать появление новых карточек заказов**;
* важно получать **только релевантные заказы**, а не весь поток;
* требуется автоматизация без ручного обновления страницы.

### 🔍 Ключевая особенность

Бот **отправляет только те заказы**, в названии или описании которых встречается ключевое слово, например:

* `Бот`

Это позволяет сразу отсекать нерелевантные заказы и получать только нужные.

---

## ⚙️ Как работает бот (кратко)

1. Бот открывает сайт в **headless‑браузере** (через Playwright)
2. Загружает HTML страницы с заказами
3. Извлекает карточки заказов (ID, заголовок, описание, ссылку)
4. Проверяет:

   * новый ли это заказ (по ID)
   * содержит ли он ключевое слово
5. Если условия выполнены — отправляет сообщение в Telegram
6. Повторяет цикл с заданным интервалом

Бот **не шлёт один и тот же заказ повторно**.

---

## 🔐 Безопасность и поведение парсера

Парсер разработан с упором на **безопасность аккаунта** и имитацию обычного пользовательского поведения, чтобы не вызывать подозрений со стороны сайта.

- Используется **Playwright и реальный браузер**, а не прямые HTTP-запросы
- Взаимодействие происходит через DOM, аналогично действиям обычного пользователя
- Между циклами проверки применяются **человеческие задержки** со случайным разбросом
- Отсутствуют агрессивные перезагрузки страницы и частые запросы
- Обрабатываются **только новые заказы**, без повторных действий
- Авторизация сохраняется и переиспользуется, без постоянных повторных логинов

Парсер ведёт себя как пользователь, который периодически обновляет страницу и просматривает новые заказы.  
При разумных интервалах опроса это **минимизирует риск ограничений или блокировок аккаунта**.

> ⚠️ Рекомендуется не занижать интервалы обновления слишком сильно и, при необходимости, использовать отдельный аккаунт для автоматического мониторинга.

---

## 🧱 Архитектура проекта

Проект разделён на логические модули:

```
project/
│
├── main.py            # Точка входа
├── parser.py          # Парсинг сайта и извлечение заказов
├── bot.py             # Отправка сообщений в Telegram
├── config.py          # Все настройки проекта
├── storage.py         # Хранение ID уже обработанных заказов
├── requirements.txt   # Зависимости
└── README.md          # Документация
```

## 🛠 Используемые технологии

| Технология           | Назначение                               |
| -------------------- | ---------------------------------------- |
| **Python 3.10+**     | основной язык проекта                    |
| **Playwright**       | эмуляция браузера и загрузка JS‑контента |
| **asyncio**          | асинхронная работа                       |
| **Telegram Bot API** | отправка уведомлений                     |
| **JSON**             | хранение обработанных заказов            |

---

## 📥 Установка

### 1️⃣ Клонировать проект

```bash
git clone <repo_url>
cd project
```

### 2️⃣ Установить зависимости

```bash
pip install -r requirements.txt
```

### 3️⃣ Установить Playwright

```bash
playwright install
```

---

## 🔧 Настройка

Открой файл `config.py` и укажи:

```python
BOT_TOKEN = "твой_telegram_bot_token"
CHAT_ID = "твой_chat_id"

TARGET_URL = "https://profi.ru/backoffice/"
CHECK_INTERVAL = 30  # секунд
KEYWORDS = []
```

---

## ▶️ Запуск

```bash
python run_all.py
```

Если всё настроено правильно:

* бот запустится без ошибок
* новые заказы начнут приходить в Telegram

---

## 🧪 Фильтрация заказов

Бот отправляет заказ **только если**:

* заказ **новый** (его ID ещё не сохранялся)
* в **названии или описании** есть ключевое слово

Пример:

✅ `Разработка Telegram бота`
❌ `Создание сайта на WordPress`

---

## 🗂 Хранение состояния

Обработанные заказы сохраняются локально:

```
storage.json
```

Это защищает от:

* дубликатов
* повторных уведомлений при перезапуске
